import React from 'react'
import { useQuery } from '@apollo/react-hooks'
import gql from 'graphql'

function Home() {
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    )
}

export default Home;